Day 42:

Overview:
- Finished Training loop of MLP

Struggles:
- Did not add scaler
- float(32) conversion on tensor vs numpy array
- targets were different dimension to inputs

What I learned:
- The trainloader bug was that I had to define a DataLoader so that i can perform mini-batching. Mini batching runs gradient descent in smaller steps, which makes the gradient 'noisier' but can help with overfitting.
- Scale before converting numpy to tensor
- MLP model takes float(32) so i either had to use .float() on tensor or astype(float) on my numpy array
- To get the same dimensions on targets i had to use unsqueeze(1) when i converted them to numpy arrays

What's next:
- Finish cv, testing and record auc so i can evaluate the model