Day 40:

Overview:
- Finished PyTorch 60 min blitz
- Started implementing MLP model - split data and initialized class

Struggles:
- I started builidng a basic logistic regression instead of an MLP model
- Converted pandas DataFrames straight to tensors which is not ideal because i will need .values later on

What I learned:
- Logistic regression has just one hidden layer
- MLP is a deep learning model with many hidden layers
- Better to convert DataFrames to numpy and then to tensors

What's next:
- Read https://pythonandml.github.io/dlbook/content/multilayer_perceptrons/neural_networks_mlp_scratch_best.html#import-essential-libraries to understand what an MLP is and how to build it from scratch and then learn how to do it with PyTorch
- Finish MLP